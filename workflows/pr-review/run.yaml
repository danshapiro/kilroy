version: 1

repo:
  path: .

llm:
  providers:
    anthropic:
      backend: api

git:
  require_clean: false
  commit_per_node: false

runtime_policy:
  stage_timeout_ms: 0
  stall_timeout_ms: 300000
